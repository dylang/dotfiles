# taps
tap homebrew/binary
tap homebrew/dupes
tap josegonzalez/php
tap wilmoore/binaries

# editor / pager
install macvim
install vim
install vimpager

# programming languages
install erlang
install rust

# networking
install netcat
install ngrep

# vcs
install git

# binaries
install phantom-js
  
# utility
install ag
install ctags
install reattach-to-user-namespace
install tmux
install tree
install watch
install wget


# taps
tap homebrew/binary
tap homebrew/dupes
tap wilmoore/binaries
tap josegonzalez/php

# editor / pager
install vim
install macvim
install vimpager
install ctags

# networking
install ngrep
install netcat

# vcs
install git

# binaries
install phantom-js
  
# utility
install ag
install tree
install wget
install tmux
install watch
install reattach-to-user-namespace

